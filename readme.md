-> npm install
-> npm run dev

entry point -> main.ts  -> game.init();

////////////////////////////////////////////////////////////////////////////////////

this task shows basic loading screen first -> then shows simple gameplay render of fishes sprite

////////////////////////////////////////////////////////////////////////////////////

we can also use spritesheet using Spritesheet(texture,atlas) way for accessing animations
// check file : -> src/ui/PlayerRunningScreen.ts
// this could be the way for accessing spritesheet in case of oop. 
// will try checking ludo screen too via globals


// understanding of assetloading via global state is important ! 
